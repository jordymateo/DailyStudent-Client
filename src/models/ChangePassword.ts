export type ChangePassword = {
    currentPassword: string;
    newPassword: string;
    repeatPassword: string;
}

export type ChangePassword2 = {
    tk: string;
    newPassword: string;
    repeatPassword: string;
}
